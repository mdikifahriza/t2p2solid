-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.agenda_kegiatan (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  judul character varying NOT NULL,
  deskripsi text,
  lokasi character varying,
  tanggal_mulai timestamp with time zone NOT NULL,
  tanggal_selesai timestamp with time zone NOT NULL,
  poster_url text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT agenda_kegiatan_pkey PRIMARY KEY (id)
);
CREATE TABLE public.arsip (
  id bigint NOT NULL DEFAULT nextval('arsip_id_seq'::regclass),
  judul text NOT NULL,
  deskripsi text,
  tanggal date NOT NULL DEFAULT now(),
  file_url text NOT NULL,
  cover_url text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT arsip_pkey PRIMARY KEY (id)
);
CREATE TABLE public.berita (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  judul text NOT NULL,
  isi text NOT NULL,
  tanggal timestamp with time zone DEFAULT now(),
  link ARRAY,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT berita_pkey PRIMARY KEY (id)
);
CREATE TABLE public.galeri (
  id bigint NOT NULL DEFAULT nextval('galeri_id_seq'::regclass),
  nama text NOT NULL,
  link_drive text NOT NULL,
  tipe text DEFAULT 'folder'::text,
  created_at timestamp with time zone DEFAULT now(),
  cover_url text,
  CONSTRAINT galeri_pkey PRIMARY KEY (id)
);
CREATE TABLE public.hero_slides (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  title text NOT NULL,
  subtitle text,
  image text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT hero_slides_pkey PRIMARY KEY (id)
);
CREATE TABLE public.kajian (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  judul text NOT NULL,
  isi text NOT NULL,
  tanggal timestamp with time zone DEFAULT now(),
  link ARRAY,
  link_download text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT kajian_pkey PRIMARY KEY (id)
);
CREATE TABLE public.kategori_berita (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nama text NOT NULL UNIQUE,
  deskripsi text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT kategori_berita_pkey PRIMARY KEY (id)
);
CREATE TABLE public.link_sosmed (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nama character varying NOT NULL,
  url text NOT NULL,
  aktif boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT link_sosmed_pkey PRIMARY KEY (id)
);
CREATE TABLE public.pendaftaran_gmni (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  judul text NOT NULL,
  deskripsi text,
  link_form text,
  cover_url text,
  kegiatan_url ARRAY,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT pendaftaran_gmni_pkey PRIMARY KEY (id)
);
CREATE TABLE public.pengumuman (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  judul text NOT NULL,
  isi text NOT NULL,
  tanggal timestamp with time zone DEFAULT now(),
  link ARRAY,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT pengumuman_pkey PRIMARY KEY (id)
);
